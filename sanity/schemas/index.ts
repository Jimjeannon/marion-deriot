/**
 * Index des schemas Sanity.
 * Ajouter ici tout nouveau schema créé dans ce dossier.
 */
import { projectSchema } from './project';
import { infoSchema } from './info';

export const schemas = [projectSchema, infoSchema];
